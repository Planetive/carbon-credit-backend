[phases.setup]
nixPkgs = ["python39", "pipenv", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = ". /opt/venv/bin/activate && uvicorn fastapi_app.main:app --host 0.0.0.0 --port $PORT"

